{
  "name": "ncai-design-system",
  "version": "0.5.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "release": "pnpm typecheck && pnpm validate && pnpm test && pnpm build && pnpm smoke:pack && node scripts/publish-missing.mjs",
    "smoke:pack": "node scripts/smoke-pack.mjs",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "validate": "turbo validate",
    "preview": "pnpm --filter @ncai/design-system-preview preview",
    "changeset": "changeset",
    "version-packages": "changeset version && pnpm sync-versions",
    "sync-versions": "node scripts/sync-versions.mjs",
    "mcp": "pnpm --filter @ncai/design-system --filter @ncai/design-system-icons build && pnpm --filter @ncai/design-system-mcp start"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
